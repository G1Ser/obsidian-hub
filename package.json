{
  "name": "obsidian-hub",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "pnpm": ">=10"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "bootstrap": "pnpm install",
    "build": "pnpm -r build",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "obsidian": "1.12.3",
    "typescript": "5.9.3"
  }
}
